-- Projectile test camera disabled.
-- Keep this file only as a harmless placeholder so Rojo can overwrite the old script.
-- Projectile flight itself is not affected.

print("[ProjectileTestCamera] disabled")
